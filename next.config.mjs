/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "brigade-hocare.com",
      "m.media-amazon.com",
      "www.electroguide.com",
      "www.spice-electronics.com",
    ],
  },
};

export default nextConfig;
